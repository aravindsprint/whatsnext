diff --git a/whatsnext/whatsnext/doctype/whatsnext_notification/whatsnext_notification.json b/whatsnext/whatsnext/doctype/whatsnext_notification/whatsnext_notification.json
index e62a022..7fa4103 100644
--- a/whatsnext/whatsnext/doctype/whatsnext_notification/whatsnext_notification.json
+++ b/whatsnext/whatsnext/doctype/whatsnext_notification/whatsnext_notification.json
@@ -10,6 +10,7 @@
     "section_recipient", "recipient_number_field", "fixed_number",
     "section_template", "template", "provider",
     "field_mapping",
+    "section_attachment", "attach_document_print", "print_format",
     "section_condition", "condition"
   ],
   "fields": [
@@ -22,9 +23,12 @@
     {"fieldname": "recipient_number_field", "fieldtype": "Data", "label": "Recipient Phone Fieldname", "description": "Fieldname on the reference doctype holding the WhatsApp number (e.g. mobile_no, contact_mobile)"},
     {"fieldname": "fixed_number", "fieldtype": "Data", "label": "Or Fixed Number", "description": "Use instead of / in addition to a fieldname, e.g. for internal alerts"},
     {"fieldname": "section_template", "fieldtype": "Section Break", "label": "Message"},
-    {"fieldname": "template", "fieldtype": "Link", "label": "Template", "options": "Whatsnext Message Template", "reqd": 1},
+    {"fieldname": "template", "fieldtype": "Link", "label": "Template", "options": "Whatsnext Message Template", "reqd": 1, "description": "Template's Header Type must be 'Document' (and approved as such by Meta) for the PDF attachment below to actually be sent as the header."},
     {"fieldname": "provider", "fieldtype": "Select", "label": "Provider Override", "options": "\nMeta\nTwilio", "description": "Leave blank to use the system default provider"},
     {"fieldname": "field_mapping", "fieldtype": "Table", "label": "Template Variable Mapping", "options": "Whatsnext Notification Field"},
+    {"fieldname": "section_attachment", "fieldtype": "Section Break", "label": "Document Attachment", "collapsible": 1},
+    {"fieldname": "attach_document_print", "fieldtype": "Check", "label": "Attach Document Print as PDF", "default": "0", "description": "Generates a PDF of the reference document using the print format below and sends it as the template's document header."},
+    {"fieldname": "print_format", "fieldtype": "Link", "label": "Print Format", "options": "Print Format", "depends_on": "eval:doc.attach_document_print", "mandatory_depends_on": "eval:doc.attach_document_print", "description": "Leave blank to use the reference doctype's default print format."},
     {"fieldname": "section_condition", "fieldtype": "Section Break", "label": "Condition", "collapsible": 1},
     {"fieldname": "condition", "fieldtype": "Code", "label": "Condition (Python expression, optional)", "options": "PythonExpression", "description": "e.g. doc.status == 'Completed'. Leave blank to always fire."}
   ],
diff --git a/whatsnext/whatsnext/doctype/whatsnext_notification/whatsnext_notification.py b/whatsnext/whatsnext/doctype/whatsnext_notification/whatsnext_notification.py
index 9eefb31..6011f60 100644
--- a/whatsnext/whatsnext/doctype/whatsnext_notification/whatsnext_notification.py
+++ b/whatsnext/whatsnext/doctype/whatsnext_notification/whatsnext_notification.py
@@ -1,5 +1,15 @@
+import frappe
 from frappe.model.document import Document
 
 
 class WhatsnextNotification(Document):
-	pass
+	def validate(self):
+		if self.attach_document_print and self.template:
+			header_type = frappe.db.get_value("Whatsnext Message Template", self.template, "header_type")
+			if header_type != "Document":
+				frappe.throw(
+					f"Template '{self.template}' has Header Type '{header_type}', not 'Document'. "
+					"'Attach Document Print as PDF' only works with a template whose header type is "
+					"Document (and which has been approved by Meta as such) — otherwise the PDF has "
+					"nowhere to go in the outbound message."
+				)
diff --git a/whatsnext/whatsnext/notification_engine.py b/whatsnext/whatsnext/notification_engine.py
index 045d34d..237b939 100644
--- a/whatsnext/whatsnext/notification_engine.py
+++ b/whatsnext/whatsnext/notification_engine.py
@@ -69,6 +69,10 @@ def _fire(notif, doc):
 			value = doc.get(row.source_fieldname, value)
 		params[row.template_variable] = value
 
+	header_media_url = None
+	if notif.attach_document_print:
+		header_media_url = _attach_document_pdf(notif, doc)
+
 	from whatsnext.whatsnext.api import send_template_message
 	send_template_message(
 		to=number,
@@ -77,4 +81,25 @@ def _fire(notif, doc):
 		provider=notif.provider or None,
 		reference_doctype=doc.doctype,
 		reference_name=doc.name,
+		header_media_url=header_media_url,
 	)
+
+
+def _attach_document_pdf(notif, doc):
+	"""Renders doc's print format to PDF and saves it as a public File so its
+	URL can be handed to Meta/Twilio as the template's document header — a
+	private file URL (the frappe.utils.file_manager default) isn't fetchable
+	by either provider, same failure mode as the scontent.whatsapp.net links
+	on the marketing templates. Mirrors the old WhatsApp Notification's
+	attach_document_print behaviour, minus the reliance on the desk-only
+	print PDF endpoint that doctype no longer has access to here."""
+	from frappe.utils.file_manager import save_file
+	from frappe.utils.pdf import get_pdf
+	from frappe.www.printview import get_html
+
+	html = get_html(doctype=doc.doctype, name=doc.name, print_format=notif.print_format or None, no_letterhead=0)
+	pdf_content = get_pdf(html)
+
+	fname = f"{doc.doctype}-{doc.name}.pdf".replace(" ", "-").replace("/", "-")
+	file_doc = save_file(fname, pdf_content, doc.doctype, doc.name, is_private=0)
+	return file_doc.file_url if file_doc.file_url.startswith("http") else frappe.utils.get_url(file_doc.file_url)
